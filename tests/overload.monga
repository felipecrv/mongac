float sum(float a, float b) {
    return a + b;
}

int sum(int a, int b) {
    return a + b;
}

int sum(int a, int b, int c) {
    return a + b + c;
}

int main() {
    int a, b, c, d;
    float fa, fb, fc;
    d = sum(a, b, c);
    /*d = sum(a, b);*/
    fc = sum(fa, fb);
    return 0;
}
