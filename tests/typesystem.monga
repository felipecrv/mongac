int a, b;
float fa, fb;
char c;
char[] s1, s2;

/* empty functions */
void func_a() {
}

void func_b() {
    return;
}

/* can declare here too */
float[][] matrix;
char[][] many_strings;

/* a sum function (shadowing previous symbols) */
int sum(int a, int b) {
    return a + b;
}

/* a big function */
float big_function(float a, float b) {
}

int main(int argc, char[][] argv) {
    float fa;
    fa = big_function(fa, 0.4);
    func_b();

    if (a == b) {
        int i;
        while (a < b) {
            a = a + 1;
        }
        matrix = new float[][10];
        many_strings = new char[][10];
        i = -1;
        while (i < 10 - 1) {
            matrix[i + 1.0] = new float[10];
            many_strings[i + 1] = "string";
        }
        return sum(a, a);
    }

    if (!(2.0 < fa) || (10.0 > fa) && a <= 10) {
        fa = -fa - 2.0;
    } else {
        fa = fa * fb;
        fa = fb / fa;
        a = 10 / 2;
    }

    return sum(a, b);
}

